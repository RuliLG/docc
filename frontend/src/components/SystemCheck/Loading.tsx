import GenericLoading from '../Loading';

export default function Loading() {
  return (
    <GenericLoading title="Checking system requirements" description="Validating that AI CLI tools and TTS services are available..." />
  );
}
